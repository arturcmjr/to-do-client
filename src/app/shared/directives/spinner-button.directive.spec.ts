import { SpinnerButtonDirective } from './spinner-button.directive';

describe('SpinnerButtonDirective', () => {
  xit('should create an instance', () => {
    // const directive = new SpinnerButtonDirective();
    // expect(directive).toBeTruthy();
  });
});
